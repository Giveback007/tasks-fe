// const appDir: string;
// function log(...message: any[]): void;
function changeTheme(theme?: Theme): void;

type Globals = {
    // env: KEY & { isDev: boolean };
    // log: typeof log;
    changeTheme: typeof changeTheme;
}
